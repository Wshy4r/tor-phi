PDF slots for TOR Phi document profiles live under this folder.

The profile data is in `src/foreignPolicyDocuments.js`. When a document is marked
as "PDF slot ready", add the PDF at the listed path, then update the record to
`localPdfAvailable: true` after OCR/import is complete.
